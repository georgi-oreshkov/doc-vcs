services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      APP_USER: ${APP_USER:-app_user}
      APP_PASSWORD: ${APP_PASSWORD:-secret}
      KC_DB_PASSWORD: ${KC_DB_PASSWORD:-keycloak}
    volumes:
      - doc-vcs-postgres-data:/var/lib/postgresql/
      - ./init-postgres.sh:/docker-entrypoint-initdb.d/init-postgres.sh:ro
    ports:
      - "192.168.0.110:55432:5432"
    networks:
      - docvcs
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "192.168.0.110:16379:6379"
    volumes:
      - doc-vcs-redis-data:/data
    networks:
      - docvcs
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:latest
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - doc-vcs-minio-data:/data
    networks:
      - docvcs
    command: minio server /data --console-address ":9001"
    ports:
      - "192.168.0.110:19000:9000"
      - "192.168.0.110:19001:9001"

  keycloak:
    image: quay.io/keycloak/keycloak:26.2
    command: start-dev
    environment:
      # Initial Admin Credentials (only used on first boot)
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_ADMIN_USER:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KC_ADMIN_PASSWORD:-admin}

      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KC_DB_PASSWORD:-keycloak}

      KC_HOSTNAME: 192.168.0.110
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME_STRICT: "false"
    ports:
      - "192.168.0.110:18080:8080"
    networks:
      - docvcs
    depends_on:
      db:
        condition: service_healthy

networks:
  docvcs:
    driver: bridge

volumes:
  doc-vcs-postgres-data:
    name: doc-vcs-postgres-data
  doc-vcs-redis-data:
    name: doc-vcs-redis-data
  doc-vcs-minio-data:
    name: doc-vcs-minio-data
